# 斯俪 Slique — 设计系统（Quiet Earth 简约大地色）

> 简约大气 · 暖灰大地色 · 无衬线。
> 本规范定义全局视觉令牌；改主题只动令牌值，不动页面布局。

## 设计基调

- **极简留白**：去除彩色卡片与彩色阴影，用细线（`#e0dbd2`）与留白分隔层次。
- **暖灰大地色**：取自参考图的三个色阶 + 奶米背景，温润不刺眼，契合「优雅体态」的女性气质。
- **无衬线字体**：统一 Inter（西文）/ 系统无衬线（中文回退），靠**字重轻重对比**而非装饰做层次。
- **大字重对比**：标题用细体（300）与加粗（600）并置；数字（如体态分）用超细体（200）放大呈现。

## 色板

| 角色 | 色值 | 用途 |
|------|------|------|
| 主色 Prime | `#8f8779` | 按钮填充、强调、Tab 选中、标题强调 |
| 主色浅 | `#a89f90` | 次级强调、渐变 |
| 中调 Taupe | `#cfc8bb` | 线框、次要元素、禁用态 |
| 浅米灰 | `#ece8e0` | 标签底、浅色块、分组背景 |
| 背景奶米 | `#f7f5f0` | 全局背景 |
| 卡片 | `#fcfaf5` | 卡片/抽屉底 |
| 分隔线 | `#e0dbd2` | 细线、边框 |
| 墨色（深） | `#4a443b` | 主文字 / 标题 |
| 墨色（中） | `#6e675b` | 正文 |
| 墨色（浅） | `#9e9688` | 辅助文字、占位、图标默认 |
| 错误 | `#a85c47` | 警示（柔化砖红，融入大地色） |

> 取消原 Vitality & Grace 的赭石/珊瑚/柔金/薄荷/薰衣草等彩色，全部并入上面的暖灰色阶。

## 字体

| 角色 | 字体 | 字重 |
|------|------|------|
| display / headline | Inter | 300（细）/ 600（强调） |
| body / label | Inter | 300 / 400 / 500 |

- 西文优先 Inter，中文回退到 `-apple-system, "PingFang SC", "Microsoft YaHei", sans-serif`。
- 大写英文小标题加字间距（`letter-spacing: .2em`）作点缀，呼应参考图。

## 令牌实现

- Tailwind 令牌见 `tailwind.config.js` —— **保留原令牌名**（`primary` / `surface` / `on-surface` …），仅替换色值，因此所有页面无需改类名即可换肤。
- 字体族在 `tailwind.config.js` 的 `fontFamily` 与 `index.html` 的字体引入处统一为 Inter。
- 焦点环、动画等见 `src/index.css`。

## 布局约束（换肤不改这些）

- 11 个页面的结构、组件层级、首页 Logo（手写笔触 SVG）保持原样。
- 仅颜色与字体随令牌变化，交互、数据持久化、路由不变。
